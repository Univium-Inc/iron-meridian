{
  "name": "@iron/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "IRON MERIDIAN authoritative server: ws WebSockets, fixed 20Hz simulation, room and lobby management, serves the built client.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsup",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@iron/shared": "workspace:*",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.13",
    "tsup": "^8.3.5"
  }
}
