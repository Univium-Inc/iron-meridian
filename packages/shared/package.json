{
  "name": "@iron/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "IRON MERIDIAN shared simulation: map gen, pathfinding, combat, AI, config. Zero DOM dependencies, identical in Node and the browser.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc --noEmit -p tsconfig.json"
  }
}
